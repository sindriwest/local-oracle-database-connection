apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oracle-database-chart.fullname" . }}-test-connection"
  labels:
    {{- include "oracle-database-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oracle-database-chart.fullname" . }}:{{ .Values.database.servicePort }}']
  restartPolicy: Never
